package controller;

/**
 * Interface to represent controller.
 * It defines the promise for controlling the flow of the stock management application.
 * Implementations of this interface handles the interactions between the model and view components.
 */
public interface Controller {

  /**
   * This method starts the program in the main method.
   */
  void startProgram();
}
